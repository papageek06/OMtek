# Tests API

Structure prevue :

- `Unit/` pour les services metier
- `Integration/` pour les endpoints et regles de role

Le runner PHPUnit n est pas encore installe dans `vendor/`.
La strategie et la commande d installation sont decrites dans `../../docs/test-strategy.md`.
