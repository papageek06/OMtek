MAIL_HOST=ssl0.ovh.net
MAIL_PORT=993
MAIL_SECURE=true
MAIL_USER=alert@professionaldev.fr
MAIL_PASS=alertBerserkfan06
MAIL_ARCHIVE_FOLDER=Archive

# API Symfony locale : "symfony serve" → https://127.0.0.1:8000 | "php -S localhost:8000 -t public" → http://localhost:8000
# fetch.js : mails "alert" → POST /api/alertes ; mails CSV BACKUP → POST /api/csv-backup
API_ALERTES_URL=https://127.0.0.1:8000/api/alertes
API_CSV_BACKUP_URL=https://127.0.0.1:8000/api/csv-backup
# Optionnel : token envoyé en X-Inbound-Token sur les requêtes API
INBOUND_TOKEN=80713024-4b30-419d-9259-b31a5787d74f

LIMIT=10

